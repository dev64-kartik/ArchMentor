# @archmentor/prompts

Shared prompt fragments and rubric templates. Consumed by:

- `apps/agent` — brain system prompt, few-shots, rubric YAML defaults
- `apps/api` — report generator (M5)

Content is filled in during M2 (brain) and M5 (report).
