# @archmentor/problems

YAML problem definitions for ArchMentor interview sessions.

One file per problem, named `{slug}.yaml`. Schema and validator land in M6
(`scripts/validate_problem.py`).

Target: 8-10 problems across difficulties by end of M6.
